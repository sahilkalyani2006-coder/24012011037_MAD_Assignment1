# Small next steps

Do these only after the current version builds and runs:

1. Walk-check the UVPCE New, MBA and Nescafe entrances; correct their JSON nodes.
2. Add one missing campus location only when its label and approach are confirmed.
3. Add an accessible-route flag after paths have been surveyed.
4. Replace approximate path segments with GPS traces only if the assignment grows.

Avoid adding login, cloud storage, live tracking or a large mapping SDK to this
basic assignment. They make the project harder to learn without improving its core.
