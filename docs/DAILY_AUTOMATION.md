# Daily updates — prepared, not active

Repository name: **24012011037_MAD_Assignment1**.
Suggested time: around 7:00 PM Asia/Kolkata each day, subject to the user's preference.
GitHub is not connected, so no repository or recurring task has been created.

## Prepared instruction

Insert the actual connected owner/repository before scheduling:

> Review `<owner>/24012011037_MAD_Assignment1` from the latest main branch.
> Commit and push real available changes. If none are available, complete at most
> one applicable small task from docs/NEXT_STEPS.md. Keep this a simple Kotlin and
> XML campus route finder for Ganpat University, Kherva, with starting-point and
> destination selectors. Preserve the user's code and the exact project name.
> Use the built-in map traced from the supplied screenshots. Only change campus
> entrances and walkways when supported by supplied or verified data. Do not invent maps,
> coordinates, paths, distances, device test results or progress. Do not add
> timetable, assignment-management, contact-directory, login or backend features.
> Run relevant available checks, update README.md with the actual work and any
> remaining challenge, commit descriptively, and push. Respect branch protection;
> create a PR if required. Never force-push or overwrite other work. If missing
> verified data prevents meaningful progress, skip the commit and report what is
> needed. Return the confirmed commit/PR link and a short explanation of
> what changed so I can understand the code.

The automation cannot see unpushed Android Studio edits on a personal laptop.
Push those edits first. A day without meaningful changes should not generate an
empty commit. Pause the task once the assignment is submitted.
